<nav class="navbar navbar-fixed-top navbar-default">
    <div class="container-fluid">
        <ul class="nav navbar-nav">
            <li class="{{ app.request.get('_route') == 'home' ? 'active' }}"><a href="{{ path('home') }}">Home</a></li>
            <li class="{{ app.request.get('_route') == 'contacts' ? 'active' }}"><a href="{{ path('contacts') }}">Contacts</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a href="/"><span class="glyphicon glyphicon-user"></span>{{ displayname|upper }}</a></li>
        </ul>
    </div>
</nav>